[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-dna"
version = "0.3.1"
description = "AgentDNA: Verifiable and Auditable AI Agents"
readme = "README.md"
requires-python = ">=3.10,<3.13"
license = { text = "MIT" }

authors = [
  { name = "Aparna Ullas"}
]

dependencies = [
  "requests>=2.0.0",
  "python-dotenv>=1.0.0",
  "rubix-py>=1.0.0",
  "py-multiformats-cid>=0.4.4",
  "pyyaml>=6.0",
]

[project.optional-dependencies]
# Required for the three-tier semantic CBAC pipeline (verify_async).
# Install with: pip install agent-dna[semantic]
semantic = [
  "sentence-transformers>=3.0.0",
  "scipy>=1.10.0",
  "numpy>=1.24.0",
]

[tool.setuptools.package-data]
"agentdna" = ["token.txt", "config.json"]

[project.urls]
Homepage = "https://github.com/rubixchain/agentdna"
Repository = "https://github.com/rubixchain/agentdna"
