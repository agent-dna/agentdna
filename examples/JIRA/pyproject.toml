[project]
name = "jira-mcp-agent"
version = "0.1.0"
description = "Streamlit + MCP Jira agent, signed via AgentDNA"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36",
    "python-dotenv",
    "requests",
    "mcp>=1.0",
    "google-genai>=1.0.0",
    "agent-dna",
]

[tool.uv.sources]
agent-dna = { path = "../../", editable = true }
