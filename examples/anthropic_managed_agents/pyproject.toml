[project]
name = "anthropic-managed-agents-example"
version = "0.1.0"
description = "Document analysis pipeline using Anthropic Claude Managed Agents"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.50.0",
    "langfuse>=4.0.0",
    "streamlit>=1.36",
    "python-dotenv",
]
