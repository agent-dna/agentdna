[project]
name = "yahoo-finance-mcp-agent"
version = "0.1.0"
description = "Streamlit + MCP Yahoo Finance agent, signed via AgentDNA"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36",
    "python-dotenv",
    "mcp>=1.0",
    "google-genai>=1.0.0",
    "yfinance>=0.2",
    "agent-dna",
]

[tool.uv.sources]
agent-dna = { path = "../../", editable = true }
